# Hello MDX Blog

This is a blog post written in **MDX**. You can add React components:

And code blocks too:

```javascript
function greet(name) {
  return `Hello, ${name}!`;
}
console.log(greet('World'));
```
